# gsystem-data — نسخة الويب للوحة

السيرفر **لا يخدم ملفات `.md`** (خطأ 500). هذه المجلد يُصدَّر تلقائياً من `operating-system/` كـ JSON.

```bash
python3 scripts/sync_gsystem_web.py
```

يُشغَّل أيضاً مع الأوتوبايلوت و`team_board_refresh.py`. **يجب git push** بعد التحديث.
