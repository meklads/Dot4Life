# تقييم أداء + تشخيص موقع — GSC 2026-09-17

> مصدر: لقطات جوست من Search Console (آخر ~3 أشهر) + فحص حي للسيرفر والريبو في نفس اليوم.  
> السياسة: `QUALITY-FIRST-POLICY.md` · التجميد قائم · لا Batch 04.

## 1) حكم الأداء بجملة
الموقع **يُكتشف ويُفهرس جزئياً** (270 صفحة) و**يظهر** (~3.3K ظهور) لكن **لا يُضغط عليه** (13 نقرة فقط · CTR 0.4% · ترتيب متوسط 38.5). المشكلة ليست «لا يوجد محتوى» — المشكلة **تقنية فهرسة + جودة/عناوين + خرائط موقع ناقصة**.

## 2) أرقام الأداء (آخر 3 أشهر)
| مؤشر | القيمة | قراءة |
|--------|--------:|--------|
| النقرات | **13** | شبه صفر تجاري (تحسن طفيف عن 2 في تموز) |
| الظهور | **3.29K** | وجود ضعيف لكنه حقيقي |
| CTR | **0.4%** | عناوين/ترتيب لا يحوّلان |
| متوسط الترتيب | **38.5** | غالباً صفحة 4+ |
| مفهرس | **270** | سقف الرؤية الحالي |
| غير مفهرس | **788** | أغلبه تقني/مقصود لا «كارثة جودة» بالكامل |

استعلام ظاهر: «الحمل» · 106 ظهور · 0 نقرات — نية عريضة تنافسية بلا صفحة قريبة بما يكفي.

## 3) تفكيك «غير المفهرس» (788)
| السبب | صفحات | الحكم |
|--------|------:|--------|
| Page with redirect | **347** | غالباً طبيعي بعد إزالة `.html` — راقب لا تهلع |
| Excluded by noindex | **119** | مقصود (بوابة جودة) |
| Discovered – not indexed | **110** | ميزانية زحف / أولوية — يحتاج sitemap أقوى + روابط |
| Alternate canonical | **96** | غالباً www أو نسخ بديلة — وحّد الكانونيكال |
| **Not found (404)** | **74** | **دين تقني حقيقي — أولوية P0** |
| Crawled – not indexed | **31** | إشارة جودة/رقة — DEEPEN يساعد |
| 403 / duplicate / soft 404 / robots | 4+4+2+1 | صغير لكن يُغلق |

### أمثلة 404 مؤكَّدة حيّاً (2026-09-17)
| URL | النتيجة |
|-----|---------|
| `/home/` → `/home` | **404** |
| `/riyadh/` → `/riyadh` | **404** (الصحيح: `/cities/riyadh`) |
| `/jeddah/` → `/jeddah` | **404** |
| `/realestate.html` → `/realestate` | **404** (الصحيح: `/real-estate`) |
| `/tools/emergency-fund-calculator.html` | **404** بعد strip |
| `/blog/gcc-family-budget.html` (www) | **404** |
| `capsule.html?id=...` | روابط ديناميكية ميتة |

الصفحة الجذر `/` و`/tools/qibla` و`/blog/saudi-mortgage-guide` تعمل (200 بعد strip).

## 4) فجوة خرائط الموقع (حرجة)
| ملف | URLs في الريبو | ملاحظة GSC |
|------|---------------:|------------|
| `sitemap-content.xml` | **176** | **الوحيد المُرسل في واجهة GSC** · Last read Sep 11 · Success |
| `sitemap-main.xml` | 25 | معلن في `robots.txt` لكن غير ظاهر كمُرسل في اللقطة |
| `sitemap-tools.xml` | 35 | كذلك |
| `sitemap.xml` | 38 | كذلك |
| **فريدة تقريباً** | **~229** | |
| محتوى LIVE تقريباً في الريبو | **~327** | |
| LIVE خارج أي sitemap | **~294** | جوجل لا يُدعى لاكتشاف أغلب المقالات عبر الخريطة |

هذا يفسّر جزئياً: مفهرس 270 فقط رغم مخزون أكبر، و«Discovered not indexed» مرتفع.

## 5) تقييم الموقع بالكامل (طبقات)
### أ) تقني / زحف
- Strip `.html` يعمل للمسارات الصحيحة؛ المسارات الخاطئة تتحول لـ404 صامت.
- ازدواج محتمل apex/www ما زال يستهلك إشارات (canonical + 404 على www).
- Sitemap ناقص وغير مُرسل بالكامل في GSC.
- 74×404 ثابتة منذ أواخر يونيو بلا انخفاض.

### ب) جودة محتوى
- `real_live_deepen` الآن **34** (بعد Wave A: كان 44).
- `quality_pct` **57%** · الهدف ≥60% لفتح الدفعات.
- CSV قصير خام **68** (يشمل stubs).
- 31× «Crawled not indexed» = طبقة جودة حقيقية فوق الـ404.

### ج) نمو / منتج
- الترافيك العضوي شبه معدوم رغم أدوات ومقالات.
- الرابحون التاريخيون (قبلة · إيجار/شراء سيارة · رهن قريب من الصفحة الأولى) لم يُضاعَفوا بعد بما يكفي.
- تجميد Batch 04 صحيح: توسيع أفقي الآن يزيد غير المفهرس أسرع من النقرات.

## 6) النواقص مرتبة بالأثر
1. **404 هيكلية** (`/home` `/riyadh` `/jeddah` `/realestate` أدوات مفقودة · capsule IDs).
2. **Sitemap coverage** — ~294 صفحة LIVE بلا دعوة زحف منظمة + GSC يرى 176 فقط من content sitemap.
3. **عدم إرسال كل الـsitemaps** في GSC (أو consolidating إلى index واحد محدّث).
4. **عناوين/وصف CTR** لصفحات ظهور بلا نقر (مثل فرص AN-00: رهن · سعرات · سياحة · قبلة).
5. **DEEPEN المتبقي** 34 → ≤25 قبل أي دفعة جديدة.
6. **Crawled not indexed (31)** — تعميق + روابط داخلية للصفحات التي زحفها جوجل ورفضها.
7. **توحيد canonical/www** وإغلاق البدائل المكررة.
8. **noindex 119** — مراجعة دورية: ما يستحق الترقية بعد بوابات الجودة فقط.

## 7) خطوات التطوير الممكنة (بدون مواد جديدة)
### P0 — هذا الأسبوع (تقني)
1. تصدير قائمة الـ74×404 من GSC كاملة.
2. لكل URL: 301 إلى البديل الصحيح أو 410 إن مات عمداً · إصلاح `/home`→`/` · `/riyadh`→`/cities/riyadh` · `/jeddah`→`/cities/jeddah` · `/realestate`→`/real-estate`.
3. بناء/تحديث **sitemap index** يضم كل LIVE `index,follow` (≥300 URL) وإرساله في GSC + التأكد أن `robots.txt` يشير إليه.
4. طلب Removals/تثبيت للـ404 القديمة بعد الإصلاح إن لزم.

### P1 — أسبوعان (فهرسة + نقرات)
5. موجة B DEEPEN: أقرب ~9–15 صفحة لإيصال `real_live_deepen` ≤25.
6. إعادة كتابة title/meta لأعلى ظهور بلا نقر (تحديث AN-00 ببيانات أيلول).
7. روابط داخلية من الهوبات (health/finance/islamic/tools) إلى الصفحات ذات الظهور.
8. تصدير تبويب **Pages** من الأداء ومقارنة الرابحين vs تموز.

### P2 — بعد استقرار الفهرسة
9. معالجة عيّنة «Crawled not indexed» تعميقاً لا حشواً.
10. مراجعة noindex: ترقية فقط بعد `amer_gate` + html5 نظيف.
11. **ممنوع:** Batch 04 / عمود `/makkah/` / slugs جديدة حتى unlock البوابة.

## 8) تعريف نجاح 30 يوماً
| هدف | رقم |
|------|-----|
| 404 في GSC | **&lt; 15** |
| URLs في sitemap المُرسل | **≥ عدد LIVE الفعلي** |
| مفهرس | اتجاه صاعد فوق 270 |
| نقرات / 30 يوم | **≥ 40** (أو CTR ≥1% على أي صفحة ظهورها &gt;80) |
| `real_live_deepen` | **≤ 25** |
| `quality_pct` | **≥ 60%** |

## 9) الخلاصة التنفيذية لجوست
1. الأداء التسويقي **ضعيف جداً** رغم تحسّن طفيف منذ تموز.  
2. أكبر رافعة فورية ليست مقالات جديدة — **إصلاح 404 + توسيع sitemap + CTR على الصفحات الظاهرة**.  
3. DEEPEN Wave A صحيح ومتوافق مع البيانات؛ أكمل نحو ≤25 ثم قِس.  
4. لا توسّع أفقياً الآن: جوجل يرفض/يؤجل أسرع مما نكسب نقرات.
