# 🗞️ تقرير عامر — مهمة الساعة 9 صباحاً | 2026-06-20

**إلى:** جوست — **من:** عامر، المدير التحريري.
موافقتي تكفي (مفوَّض). نُشِر فقط ما أفخر به.

## الخلاصة في سطر
مقال جديد واحد دخل النطاق («العمرة مع الأطفال» ar+en). كان **قريباً ويحتاج صقلاً**، فحرّرته بنفسي مباشرةً واعتمدته. لا مقالات مرفوضة اليوم. شدّدت 3 قواعد.

## ما راجعته
- **مصدر التحديد:** `git log` آخر ~24 ساعة + عمود «review» في `system/tasks.json` (فارغ) + مجلد `blog/`.
- **عمود review:** لا بطاقات. **بطاقات 13–19** ما تزال `returned` منذ 06-16 ولم تُعَد صياغتها (لا تسليم جديد) → لم تُراجَع اليوم.
- **الجديد فعلاً:** «العمرة مع الأطفال» — `blog/umrah-with-kids-guide.html` + `-en.html` (مسجّل في `articles.json`، قسم featured-stories، 2026-06-20).

## ✍️ ما حرّرته بنفسي ثم اعتمدته
**العمرة مع الأطفال (عربي + إنجليزي) — ✅ معتمد بعد تحرير مباشر.**

نقاط القوة كما وصلني: 1893/1733 كلمة، H1 + 13×H2، FAQ من 5 أسئلة، **مصادر رسمية محقّقة** (وزارة الحج والعمرة، نُسك، اعتمرنا — لا أرقام مختلَقة)، CTA لأداة، 4 روابط داخلية وصفية، صورة رئيسية WebP + صورتا جسم في `assets/images/articles/`، نبرة آدمية محتشمة عائلية.

ما أصلحته قبل الاعتماد:
1. **الشرطات الطويلة (—):** كانت 5 (عربي) + 11 (إنجليزي) → **0/0**. حوّلتها لفواصل/نقاط.
2. **إخلاء المسؤولية الشرعي:** كان غائباً → أضفت إخلاءً صريحاً («معلومات عامة، ليست فتوى؛ راجع أهل العلم والمنصات الرسمية») في النسختين.
3. **Schema (JSON-LD):** كان غائباً → أضفت `Article` + `FAQPage` صحيحَي البناء (تحقّقتُ أن JSON صالح) في النسختين.

## ❌ ما رُفض
لا شيء اليوم.

## 🔁 القواعد التي شدّدتها اليوم (حلقة التحسين)
سُجّلت في `quality-log.md` وشُدّدت في `content-standards.md`:
1. **الشرطة الطويلة** رجعت للمرة الثالثة → جعلتها **بوابة نشر آلية**: `grep -c "—" = 0` شرط للاعتماد، لا اجتهاد.
2. **Schema غائبة منهجياً** (183 من 186 مقال بلا JSON-LD) → جعلت `Article` + `FAQPage` **بنداً حاكماً** في Definition of Done. مطلوبة حملة سدّ منهجية للباقي.
3. **إخلاء المسؤولية** → أضفت «شرعية/عمرة/حج» صراحةً لفئات الإخلاء الإلزامية بجوار صحة/مالية.

## ⚠️ يحتاج يدك (لا أملك صلاحيته من البيئة المعزولة)
1. **النشر (git push):** البيئة المعزولة تكتب الملفات لكن **لا تحذف ملفات قفل git ولا تملك مفتاح SSH للريموت**. تعديلاتي محفوظة في ملفاتك فعلياً، لكن الـ commit/push يلزم تنفيذه من جهازك:
   ```
   cd ~/Desktop/Turriva/d4l/Dot4Life
   rm -f .git/index.lock .git/HEAD.lock
   git pull --no-edit
   git add blog/umrah-with-kids-guide.html blog/umrah-with-kids-guide-en.html \
           operating-system/content-standards.md operating-system/quality-log.md \
           operating-system/reports/2026-06-20-amer-9am.md system/tasks.json
   git commit -m "amer: approve & polish umrah-with-kids (0 em-dashes, disclaimer, Article/FAQPage schema)"
   git push
   ```
   (لاحظ: توجد تعديلات سابقة غير مرتبطة بي على `index.html` والـstyles وملفات أخرى — راجعها وافصلها كما يناسبك.)
2. **الدَّين المنهجي:** 182 مقالاً بلا Schema. أقترح مهمة batch مخصّصة لحقن `Article`+`FAQPage` آلياً عبر `scripts/migrate-article-template.py`.

— عامر
