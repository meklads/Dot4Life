# دورة عامر — 2026-06-30 09:00 (عودة من الغياب)

## السياق
عامر غائب منذ 2026-06-24 حتى اليوم (Cursor كان QA مؤقتاً لـ D10). هذه أول مراجعة 9 صباحاً لعامر بعد العودة. تعذّر `git pull`/`push` من هذه الجلسة (فشل التحقق من مفتاح SSH لمضيف GitHub — لا بيانات اعتماد SSH في هذا الساندبوكس)؛ الفحص تم على آخر حالة محلية للشجرة (`b01de8b`، نظيفة باستثناء `handoff-tickets.json`). **العمل هنا التزام محلي فقط — الدفع (push) معلّق وبحاجة لتشغيله من الجهاز الذي يملك مفتاح SSH (وفق نمط أعطال الدفع المتكرر الموثّق في `quality-log.md`).**

## النطاق
16 مقالاً جديداً (32 ملف ع/en) من دفعتي اليوم:
- **Hermes Batch 4 (D25→D32):** emergency-fund-guide-gulf-families · hydration-guide-hot-climates-families · property-roi-comparison-saudi-uae · umrah-off-peak-seasons-guide · expat-built-life-saudi-arabia · renting-vs-buying-property-saudi-families · digital-minimalism-modern-families · power-of-patience-marriage.
- **دفعة جوست اليومية (commit `0764a3c`):** building-family-reading-habit · saving-vs-investing-families · mother-homeschooled-five-children · halal-investment-gulf-families · screen-time-eye-health-children · umrah-with-elderly-parents · power-of-i-love-you-arab-families · first-home-buyer-saudi-arabia.

## أداة الفحص
بُني سكربت فحص مستقل (`amer_gate.py`) يطبّق بنود `content-standards.md` كاملة: عدد الكلمات ≥1300، شرطات طويلة=0، Article+FAQPage JSON صحيح، FAQ 4-6، كليشيهات AI، اقتباس سلطة مختلَق، إخلاء مسؤولية للحسّاس، بوابة الرابط العميق لكل نسبة مئوية (قاعدة 2026-06-24)، ادّعاء سلطة بلا رابط مجاور، صور Unsplash، روابط داخلية، وخلط لغوي بالصفحة العربية.

## النتيجة: 0 / 32 ملف يجتاز البوابة. القرار: ❌ رفض الجميع — لا نشر، لا LIVE.

هذه ليست حالة "صقل بسيط" — كل ملف فيه 3 إلى 7 مخالفات حاكمة متزامنة. وفق ميثاق جوست (بند 0 وقسم سلطة التدخل)، خطأ بهذا الحجم والانتشار يُصنَّف "خطأ كبير/بنيوي" يُرجَع لرواق/Hermes، لا يُرقَّع بتدخل مباشر يخفي العيب الجوهري (خاصة الأرقام المالية/الصحية غير الموثّقة بروابط).

### المخالفات المتكرّرة عبر الـ32 ملفاً (بالعدد)
| المخالفة | عدد الملفات المتأثرة |
|---|---|
| شرطات طويلة (—) > 0 | 32 / 32 |
| نِسَب مئوية بلا أي رابط عميق واحد | 22 / 32 (حتى 50 نسبة في ملف واحد) |
| إخلاء مسؤولية مفقود (محتوى مالي/صحي/شرعي) | 20 / 32 |
| ادّعاء سلطة (جامعة/مجلة/منظمة/دراسة) بلا `href` مجاور | 18 / 32 |
| صورة Unsplash placeholder (لا hero معتمد) | 9 / 32 (لا صورة hero موجودة لأي من الـ16 سلَج في `assets/images/`) |
| كلمات الجسم < 1300 | 7 / 32 |
| **عيب حرج منفرد:** `health/screen-time-eye-health-children.html` (الصفحة العربية، `lang="ar"`) جسمها **إنجليزي بالكامل** — نفس عدد الكلمات (3233) ونفس الفقرات حرفياً مثل التوأم الإنجليزي. هذا تكرار حرفي لعطل `digital-minimalism-families` (يونيو 24) رغم 6 جولات تشديد سابقة لنفس العطل. | 1 (لكنه الأخطر) |
| **عيب حرج منفرد:** `featured-stories/mother-homeschooled-five-children-en.html` بلا أي `FAQPage` schema إطلاقاً (FAQ=0) | 1 |

### حالة لوحة المهام قبل الفحص (مشكلة عملية إضافية)
الكروت D25→D32 في `handoff-tickets.json` كانت موسومة `"col": "done"` (أي "منتهي LIVE — منشور على الموقع") من Hermes·بناء **مباشرة بعد البناء، بلا مرور ببوابة عامر** — خرق صريح لقاعدة 2026-06-21 الحاكمة ("لا يُنقل أي ملف لقسم عام قبل مرور بوابة عامر"). التحقّق: الملفات **غير مدرجة بعد** في `sitemap.xml`/`sitemap-content.xml` ولا في `site-sections.json`، فالخطر الفعلي محدود (لا فهرسة بعد) لكن المسار غير سليم. **صُحِّحت الكروت الثمانية الآن:** `col` أُعيد إلى `hermes_write`، `qa_status=rejected` مع ملاحظات بند-بند لكل سلَج.

## القرار النهائي
- **لا اعتماد لأي من الـ16 مقالاً.** تبقى خارج `sitemap`/`site-sections` (صحيح، لا تغيير).
- **إرجاع للكاتب (Hermes·كتابة وتحرير):** كل ملف بملاحظاته في الجدول أعلاه + بطاقات `handoff-tickets.json` المحدّثة (D25→D32). دفعة "تحديث جوست اليومي" (D17-D24 تقريباً) ليس لها بطاقات في هذا الملف — تحتاج Hermes·تنسيق لفتح بطاقات مقابلة قبل إعادة التسليم.
- **لا تدخل تحرير مباشر من عامر هذه الدورة:** حجم المخالفات لكل ملف (حتى 50 نسبة بلا مصدر) يتجاوز "صقل" ويستلزم إعادة كتابة فعلية للادّعاءات الرقمية، وهذا عمل الكاتب لا تصحيح آلي من عامر (يطابق سابقة Batch 02، 2026-06-24).
- **لا توليد صور Higgsfield هذه الدورة:** لا فائدة من توليد hero لمقال محجوب نصياً (نفس سياسة 2026-06-24 20:00).

## التشديد الحاكم الجديد (يُضاف الآن لـ `content-standards.md`)
١. **بوابة ما-قبل-التسليم إلزامية لـ Hermes·بناء:** أي بطاقة `*-BUILD` يُمنع وضعها `col: done` مباشرة — يجب أن تمر أولاً بعمود `amer` (أو ما يعادله) وتنتظر `qa_status=approved` قبل أي ترقية لـ`done`. خرق هذه القاعدة وقع 8 مرات متتالية اليوم (D25→D32 كلها).
٢. **فحص ترجمة آلي إلزامي قبل البناء:** أي صفحة `lang="ar"` يجب أن يجتاز جسمها فحص "نسبة الأحرف العربية إلى اللاتينية" (≥80% عربي في الفقرات الحقيقية) قبل أي `commit` — لمنع تكرار عطل screen-time-eye-health-children (تطابق حرفي مع توأمه الإنجليزي، وهو نفس عطل digital-minimalism من 6 دورات مضت).
٣. **سقف فعلي لا توصية فقط:** أي مسوّدة فيها أكثر من 3 نِسَب مئوية دقيقة يجب أن **ترفضها بوابة Hermes الذاتية قبل التسليم** (وليس فقط بوابة عامر بعد البناء) — التكرار اليوم في 22/32 ملف يثبت أن التشديد السابق (2026-06-24) لم يُطبَّق فعلياً على خط الإنتاج، فقط على المراجعة بعد الحقيقة.

## ملاحظة على البنية التحتية
الدفع لـ GitHub معطّل من هذا الساندبوكس (لا مفتاح SSH). كل تعديلات اليوم (handoff-tickets.json + هذا التقرير + quality-log.md) محلية فقط حتى تُدفع من جهاز يملك صلاحية SSH (كرون الماك المذكور سابقاً في السجل). لم تُجرَ أي محاولة Bypass للقيود الأمنية.
