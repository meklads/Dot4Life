# P0 SEO Fix — 404 redirects + full LIVE sitemap — 2026-09-17

> أمر جوست: تنفيذ P0 من `site-performance-audit-2026-09-17.md`

## ما نُفِّذ

### 1) تحويلات 404 المعروفة (stubs + قواعد)
ملفات إعادة توجيه `noindex` + `location.replace` + canonical:

| من | إلى |
|----|-----|
| `/home` | `/` |
| `/riyadh` `/jeddah` `/dubai` `/abu-dhabi` `/oman` | `/cities/...` |
| `/realestate` | `/real-estate` |
| `/tools/emergency-fund-calculator` | `/finance-wealth/emergency-fund-guide-gulf-families` |
| `/blog/gcc-family-budget` | `/blog/gcc-family-budget-2025` |

أيضاً: `legacy-404-redirects.json` · `_redirects` (301 أولاً) · `.htaccess` · `serve.json`.

### 2) Sitemap كامل بمسارات نظيفة (apex)
| ملف | URLs |
|-----|-----:|
| `sitemap-main.xml` | 21 |
| `sitemap-content.xml` | **321** (كان 176) |
| `sitemap-tools.xml` | 27 |
| **المجموع عبر index** | **369** |
| `sitemap-index.xml` | جديد — أرسله في GSC |

- الدومين: `https://dotforlife.com` (لا www)
- المسارات: بدون `.html` لتقليل «Page with redirect» في GSC
- يستبعد: `noindex` · stubs · refresh redirects

### 3) سكربتات
- `scripts/rebuild_live_sitemaps.py` — إعادة بناء السايتماب من LIVE
- `scripts/generate_static_routes.py` — يحترم legacy 301

## مطلوب من جوست في GSC (دقيقة واحدة)
1. **Sitemaps → Add:** `https://dotforlife.com/sitemap-index.xml`
2. (اختياري) أبقِ `sitemap-content.xml` أو احذفه بعد نجاح الـindex
3. بعد انتشار GitHub Pages (~2–5 دقائق): Validate 404 في تقرير الفهرسة لاحقاً

## تحقق بعد الدفع
```bash
curl -sI -L https://dotforlife.com/home | head
curl -sI -L https://dotforlife.com/riyadh | head
curl -sI https://dotforlife.com/sitemap-index.xml | head
curl -s https://dotforlife.com/sitemap-content.xml | rg -c '<loc>'
```
المتوقع: `/home` و`/riyadh` يصلان 200 على الهدف · content ≥300 loc.
