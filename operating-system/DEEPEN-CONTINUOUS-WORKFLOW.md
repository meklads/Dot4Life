# DEEPEN — مسار متواصل (2026-06-25)

> قرار جوست: أقصى إنتاج اليوم مع الحفاظ على الجودة عبر مراجعة متوازية.

## الأدوار

```
هيما (N) ──► Cursor acting QA (A) ──┬── PASS ──► Cursor build+صور+push (C)
                                    └── RETURN ──► deepen-fix-queue.md
```

- **عامر:** غائب حتى 2026-06-30 — Cursor يغطي بوابة A بنفس معايير `amer-mandate`.
- **جوست:** لا تدخل يومي — Cursor يرفع ويُطلق الدفعات التالية آلياً.

## هيما

1. سكيل **الكتابة فقط** — لا تحليل، لا نمو.
2. أكمل **الدفعة الحالية كاملة** ملفاً ملفاً (D14 = 10 · D15 = 20).
3. عند انتهاء الدفعة: اكتب في TEAM-BUS `D14 منجز` أو `D15 منجز`.
4. **قانون الراحة (D15 فقط):** بعد كل **5 مقالات** خذ **5 دقائق** ثم أكمل الـ5 التالية.
5. عند **50 مقالاً LIVE** في هذه الحملة: **توقّف** وارجع لـ `deepen-fix-queue.md`.

## Cursor acting QA

1. راجع كل ملف فور تسليمه (لا انتظار نهاية الدفعة).
2. **APPROVED LIVE:** أصلح تقني/صور → `fix_en_toc.py` على EN → `DXX-XXC` → commit + push.
3. **RETURN:** سجّل في `deepen-fix-queue.md` — لا توقف هيما.
4. عند اكتمال دفعة من هيما ونشرها: شغّل `python3 scripts/launch_deepen_pending.py` إن وُجدت دفعة في `deepen-launch-queue.json`.
5. عند العداد = 50: أوقف إطلاق دفعات جديدة؛ أرسل طابور الإصلاح.

## معيار البوابة

- AR ≥1600w · EN ≥1500w إن وُجد
- FAQ ≥4 · Article + FAQPage JSON-LD صالحان · H2 ≥6
- 0 em-dash · 0 Unsplash
- أي مؤسسة/جامعة/منظمة/مجلة أو رقم دقيق → رابط عميق **أو** صياغة وصفية
- **[أُضيف 2026-07-09] صفر اقتباس ديني مباشر — قاعدة عامر المطلقة 2026-07-02:** ممنوع أي حديث نبوي أو آية قرآن بالنص، عربي أو إنجليزي، منسوب أو غير منسوب، بأي صياغة ("قال النبي..."، "Allah says..."، "Prophet said..."، أرقام آيات Quran X:Y). البديل الإلزامي: صياغة قيم عامة بلا نص محدد ("من قيمنا وثقافتنا..."). **هذا البند يشمل أي دفعة DEEPEN تُستأنَف أو تُعاد معالجتها من محتوى/مسودات قديمة سابقة لـ 2026-07-02** — السبب المباشر لهذه الإضافة: اكتُشف مرتين هذا الأسبوع أن دفعات "استعادة"/DEEPEN أعادت إدخال اقتباسات كانت أُزيلت سابقاً، لأنها سحبت من مصدر/backup قديم لا يعرف هذه القاعدة. `amer_gate.py` أصبح الآن (commit `6d75ccdc`) يفحص هذا البند آلياً ويُفشل الملف تلقائياً — لا عذر لعدم الالتقاط.

## الصور (Cursor)

1. `assets/images/approved/` أولاً
2. إعادة استخدام داخلي مناسب
3. ستوك مجاني موثق عند الضرورة فقط

## المراجع

- `operating-system/reports/deepen-batch-11.md` (الدفعة الحالية)
- `operating-system/deepen-fix-queue.md` (طابور الإصلاح)
- `operating-system/WRITING-LAW.md`
