# 🧠 مذكّرة تسليم الجلسة — DotForLife (عامر ↔ جوست) · 2026-07-01

> **الغرض:** الصقها في بداية أي سيشن جديد فيتذكّر عامر السياق وأهم ما فيه فوراً.
> ⚠️ **هذه خريطة لا أرض:** تصف الوضع وقت الكتابة، لا الوضع الآن. **لا تصدّقها كما هي** — طبّق قاعدة التحقق المستقل عليها نفسها (قسم 0).

---

## 0) تحقّق أول ما يتصل الفولدر (أعِد تأسيس الأرض الحقيقية قبل أي كلام)
شغّل هذه فوراً لتعرف الحالة **الآن**، لا حالة وقت كتابة المذكّرة:
```bash
cd <repo> && export GIT_DISCOVERY_ACROSS_FILESYSTEM=1
git log --oneline -15                          # ماذا حدث بعد المذكّرة؟
pip install html5lib -q                          # ضروري للفحص البنيوي
python3 scripts/structural_audit.py              # هل من سايدبار مكسور الآن؟
# هل أصلح هيرمز الـ12 ملف من دفعة 34592c2؟
for f in $(git show --name-only --format="" 34592c2 | grep '\.html$'); do [ -f "$f" ] && echo "$(python3 scripts/amer_gate.py "$f" 2>&1 | grep -oE '^(PASS|FAIL|WARN)' | head -1) $f"; done
```
النتيجة = أرضك الحقيقية. ابنِ عليها، لا على نص القسم 6.

---

## 1) من نحن
- **جوست** = صاحب منصة **DotForLife** (`dotforlife.com`) — منصة عائلية إسلامية خليجية ثنائية اللغة (عربي/إنجليزي).
- **عامر (أنا)** = المدير الفني للجودة + المستشار الأمين (أعمل في Cowork).
- **هيرمز** = وكيل الكتابة (يكتب 8 مقالات/يوم). **الأوتوبايلوت (GSystem)** = آلي. **بوابة CI** = فحص جودة آلي.
- **الريبو:** `/Users/ghousemac/Desktop/Turriva/d4l/Dot4Life` (GitHub: `meklads/Dot4Life`).

## 2) القواعد الحاكمة (لا تتغيّر)
- **قاعدة 0:** أبدِ رأيك **قبل** تنفيذ أي مهمة لها وزن/كلفة/يصعب التراجع عنها. التافه القابل للتراجع → نفّذ بسطر رأي.
- **صدق وصراحة:** **لا تصدّق تقرير أي وكيل («تم»)** — تحقّق مستقل دائماً (git/الملف الفعلي). جوست يقدّر الصدق فوق المديح.
- **أنا أكتب رسائل الفريق التقنية**، جوست ينقلها.
- **الجودة قبل الكمية**، دائماً.
- **الاحتشام خط أحمر:** حجاب/خمار **لا نقاب**، لا خمر/خنزير/قمار.
- **توفير الكريديت:** اعتمد على الأتمتة المجانية + فحوص محلية رخيصة (git/python) بدل المتصفّح، اشتغل دفعات، لا babysitting.
- **أمان:** لا تُدخِل باسوردات/مفاتيح API أبداً.

## 3) عمل git (مهم جداً)
- الساندبوكس **يـcommit لكن لا يـpush/pull** (جوست يرفع). كل commit يحتاج: `export GIT_DISCOVERY_ACROSS_FILESYSTEM=1` + `git config user.email/name` + `rm -f .git/*.lock`.
- **مشكلة تزامن خطيرة:** جلسات متعددة (أنا + هيرمز + أوتوبايلوت + CI) تكتب معاً → تعديلات تضيع، أقفال، rebase متكرر. **الحل المؤجّل: تسلسل الجلسات.**
- الرفع عند التباعد: `git pull --rebase origin main && git push` (اعمل stash لو الشجرة متسخة).

## 4) ما أُنجز هذه الجلسة
1. **إصلاح عطل «السايدبار تحت المقال»** جذرياً (وسوم HTML غير مقفولة تبلع السايدبار). **صُلّح 50 مقالاً** (34+16) + **أُعيد بناء ملفين إنجليزي متلفين**. الأداة: إعادة موازنة الوسوم بـ`html5lib`.
2. **فحص بنيوي في `scripts/amer_gate.py`** (`sidebar_structure_fail`): السايدبار لازم طفل مباشر لـ`article-layout`. مربوط في بوابة CI (`quality-gate.yml` تثبّت html5lib).
3. **تدقيق بنيوي أسبوعي مجاني** (`scripts/structural_audit.py` + `weekly-structural-audit.yml`).
4. **نموذج ذاتي التشغيل:** حلقة تصحيح هيرمز الذاتية (يصلح المعزول أولاً) + عامر = تدقيق دوري لا مُصحّح كل ملف.
5. **ميثاق هيرمز** (`operating-system/HERMES-CHARTER.md`) — العقد التأسيسي (هوية · فلسفة الطبقتين · بروتوكول التحقق §5 · بار 15 بند). **هيرمز قرأه وأثبت فهمه** (رجّع §5 صح).
6. **نظام الصور:** فولدر `assets/images/pending-review/` + **40 برومبت Ideogram بمفهوم «الحياة»** + تحديث `VISUAL-DIRECTION.md`.

## 5) الدرس المؤسِّس للجلسة
هيرمز ادّعى «17/17 نجاح» والواقع **«12/14 فشل»** (دفعة commit `34592c2`) — لأن بيئته بلا `html5lib` فالفحص البنيوي **تخطّى بصمت**. الدرس → **بروتوكول §5:** قبل أي فحص: `git pull` + `pip install html5lib` + **الصق ناتج الطرفية الحقيقي** (لا «تم»). **البوابة المستقلة = خط الدفاع؛ الميثاق يرفع السقف، البوابة تحمي الجمهور.**

## 6) رسالة تسليم من عامر (الجلسة السابقة) إلى عامر (السيشن الجديد) — بالنص من جوست
> إنت أعدت بناء الأرض الحقيقية صح — كمّل عليها. دي إضافات وتصحيحات بعد المذكّرة:
> 1. **تصحيح لا تبني نظرية غلط عليه:** الصورتان `01-savings.png.png` و`02-health.png.png` = من إنتاج جوست نفسه (ولّدهما من برومبتات الصور)، محفوظتان بامتداد مزدوج بالغلط. ليستا دليلاً على دفعة نص+صور مجهولة. تطابق الموضوع صدفة.
> 2. **تغييرات غير مُلتزَمة في الشجرة (التزمها عند أول كتابة — ملفات آمنة لا يلمسها هيرمز):** `assets/images/pending-review/image-prompts-batch-01.md` (40 برومبت بمفهوم «الحياة» + تعديل احتشام: شعر النساء مغطّى بالكامل، لا يظهر شعر المقدّمة) و`operating-system/VISUAL-DIRECTION.md` (نفس التحديث).
> 3. **قرار جوست في التجميد:** راجع الـ8 سلَج المجمّدة ومرّرها على `amer_gate.py`. لا تقبل مجهول المصدر تلقائياً — يمرّ فقط لو (عدّى البوابة + محتشم + على الهوية + يمكن ربطه بتذكرة)، وإلا احذفه.
> 4. **هيرمز:** الميثاق (`HERMES-CHARTER.md`) نافذ وهيرمز أثبت قراءته (رجّع §5 صح). لسه ما صلّحش الـ12 من دفعة `34592c2`. عند «تم»: تحقّق مستقل (`git pull` + `pip install html5lib` + `amer_gate.py` على الملفات الملتزَمة فعلاً + الصق الناتج). لا تدعه يلمس الملفين الناجحين. الملف الملوّث (644 كلمة) = إعادة كتابة كاملة لا رقعة.
> 5. **أولوية: خلل بوابة CI.** لم تعزل الـ12 المكسورين — على الأرجح `html5lib` غير مثبّت وقت الـpush، أو نسخة قديمة. إصلاحها أهم شيء لأن النموذج الذاتي لا يحمي فعلاً بدونها.
> 6. **التزامن:** دورة عامر آلية كل ~30 دقيقة + أوتوبايلوت + إنت = تصادم. نظّف الأقفال، rebase بحذر، والأفضل تسلسل الجلسات. الساندبوكس يـcommit، جوست يـpush.
> 7. **السلوك الحاكم:** قاعدة 0 · صدق (تحقّق لا تصدّق «تم») · أنت تكتب رسائل الفريق وجوست ينقلها · الجودة قبل الكمية · الاحتشام خط أحمر (الآن: شعر النساء مغطّى بالكامل) · وفّر الكريديت.

### تحديث حيّ من نفس الجلسة (بعد الرسالة أعلاه مباشرة) — لا تعتمد على الرسالة وحدها
- **بند 2 (الالتزام) تم فعلاً:** التزمت `image-prompts-batch-01.md` + `VISUAL-DIRECTION.md` **فقط** بـ commit `64bbd62` (2 ملف بالضبط، تحقّق: `git show --stat 64bbd62`). الـ16 ملف المجمّدة والصورتان بقيا untracked عمداً.
- **بند 4 (هيرمز) تغيّر جزئياً — تحقّق حيّ الساعة ~15:38 UTC:** فيه commit جديد `fb1ac29` ("D01+D02 EN+AR — amer_gate.py PASS"). فحصته بنفسي بـ`amer_gate.py` (لا تصديق): **D01 (`digital-minimalism-faith-families` ع+en) و D02 (`mindful-family-meal-nutrition-faith` ع+en) = 4/4 PASS فعلاً**، بما فيها الملف الملوَّث سابقاً (الآن 1302 كلمة نظيفة). **إجمالي دفعة الـ14 الآن: 8 فشل / 6 نجاح** (تحسّن من 12/2). الباقي الفاشل: 4 EN ببنية سايدبار مكسورة + 4 AR بنفس عطل WHO/Unsplash/لاتيني. **أعد الفحص بنفسك عند وصولك — الأرقام دي كانت صح وقت الكتابة فقط.**
- **⚠️ حادثة تزامن فعلية حصلت أثناء تنفيذ بند 2:** أول محاولة `git add <2 files> && git commit` انسحب فيها تلقائياً 21 ملف (الـ16 المجمّدة + الصورتان + ملفات تانية) بسبب index متسخ من عملية `amer-bot` متزامنة. **اتصلح فوراً بـ`git reset --soft HEAD~1` + `git reset` (mixed) قبل أي push** — الـcommit الغلط ما اتدفعش أبداً، ما فيش أثر بره الريبو. **الدرس المؤكَّد عملياً:** استخدم `mv` لا `rm`/`unlink` على أي `.git/*.lock` (فشل unlink دائم على هذا المونت)، وتحقّق دايماً بـ`git show --stat HEAD` بعد أي commit إنه يحتوي **فقط** الملفات المقصودة قبل ما تكمل — لا تفترض إن `git add <files>` هيقتصر على اللي كتبته لو فيه عملية تانية شغالة.

## 7) المُعلّق / الخطوات التالية (بترتيب الأولوية — حدّثها بتحقّق حيّ، لا تنسخها)
- **هيرمز يكمّل الباقي (8 ملفات فاشلة من الـ14):** 4 EN (بنية سايدبار) + 4 AR (WHO بلا رابط + Unsplash + لاتيني). D01+D02 اتصلحوا وتحقّقوا مستقلاً — لا تعيد فتحهم. **لا تدع أحداً يلمس الملفين الناجحين الأصليين** (`engineer-simplified-family-life-en`, `power-of-i-was-wrong-en`).
- **تجميد الـ8 سلَج/16 ملف:** طبّق قرار جوست في القسم 6 أعلاه — راجع، مرّر على `amer_gate.py`، اقبل أو احذف حسب الشروط الأربعة. لسه معلّق، لا رد نهائي.
- **إصلاح خلل بوابة CI:** لماذا لم تعزل الـ12 (أصلاً 8 الباقيين) تلقائياً؟ (الأرجح: `html5lib` غير مثبّت وقت الـpush أو نسخة قديمة). خطة: فحص قراءة-فقط ثم إصلاح وقت هدوء الريبو.
- **git:** تحقّق `git log --oneline origin/main -1` مقابل `HEAD` لمعرفة عدد الـcommits غير المدفوعة الحالي (كان يتغيّر باستمرار بسبب أوتوبايلوت/amer-bot). ادفع لو مستقر، أو وثّق التأخير.
- **الصور (الدفعة الجديدة، غير الـ01/02 المذكورتين في القسم 6):** جوست يولّد من الـ40 برومبت (مفهوم الحياة، شعر مغطّى بالكامل). عند «الصور جاهزة» → راجع (احتشام/هوية/جودة) → المقبول لـ`assets/images/approved/` + المانيفست → الأوتوبايلوت يحقن. **تنبيه دائم: احفظها بامتداد `.png` واحد** (مشكلة `.png.png` تتكرر).
- **تسلسل الجلسات** لحل التزامن — **مؤكَّد عملياً هذه الجلسة إنه خطر حقيقي لا نظري**، فيه `amer-bot` آلي يكتب في نفس الريبو أثناء عملك مباشرة. راجع `AMER-ORDERS-ACTIVE.md`/`quality-log.md` قبل أي قرار — قد تكونا أحدث من هذه المذكّرة.

## 7) ملفات مفتاحية
- `operating-system/HERMES-CHARTER.md` — العقد التأسيسي لهيرمز.
- `operating-system/HERMES-ACCEPTANCE-CRITERIA.md` — 15 بند + مؤشّر للميثاق.
- `operating-system/content-standards.md` — المعايير التفصيلية.
- `operating-system/VISUAL-DIRECTION.md` — توجيه الصور (الآن: مفهوم الحياة).
- `scripts/amer_gate.py` — البوابة (فيها الفحص البنيوي).
- `scripts/ci_quality_gate.py` + `.github/workflows/quality-gate.yml` — بوابة CI.
- `scripts/structural_audit.py` + `.github/workflows/weekly-structural-audit.yml` — التدقيق الأسبوعي.
- `scripts/migrate-article-template.py` — هجرة القالب (مؤمَّنة ببوابة بنيوية).
- `assets/images/pending-review/image-prompts-batch-01.md` — الـ40 برومبت.

## 8) أداة التحقق البنيوي السريعة (أعِد بناءها عند الحاجة)
```python
import html5lib
def sidebar_ok(html):
    doc=html5lib.parse(html, treebuilder="dom"); A=[]
    def w(n):
        for c in n.childNodes:
            if c.nodeType==1:
                if 'article-sidebar' in (c.getAttribute('class') or ''): A.append(c)
                w(c)
    w(doc)
    return bool(A) and 'article-layout' in (A[0].parentNode.getAttribute('class') or '')
```
